import { render, screen } from '@testing-library/react';
import {{pascalCase name}} from '.';

describe('{{pascalCase name}}', () => {
  it('should render the text component {{pascalCase name}}', () => {
    render(<{{pascalCase name}} />);

    expect(
      screen.getByText('This component {{pascalCase name}} was created!')
    ).toBeInTheDocument();
  });
});
